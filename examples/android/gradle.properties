org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# Required when packages/kotlin is included as a subproject (its
# gradle.properties are not loaded automatically).
VERSION_NAME=0.9.0
GROUP=io.github.davidamunga
POM_ARTIFACT_ID=kenya-locations
